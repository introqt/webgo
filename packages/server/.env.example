# Server
PORT=
NODE_ENV=development

# Database
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_SSL=
# DB_SSL_CA=/path/to/ca-certificate.crt  # Optional: Custom CA certificate for SSL in production

# JWT
# SECURITY: Generate a strong secret for production with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# REQUIRED in production (minimum 32 characters)
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=
